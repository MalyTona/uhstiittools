# Branding images

- `uhst-logo.png` is the institutional logo supplied by the approved Figma design.
- `figma-background.png` is the subtle repeating background pattern supplied by that design.

Keep these assets local so the production interface has no external runtime dependency.
